strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# Choose ONE model provider below based on your setup:

# Option A: AWS Bedrock (requires AWS credentials)
# boto3>=1.34.0

# Option B: Local Ollama
# (no extra pip package needed, just run Ollama separately)

# Option C: LiteLLM (OpenAI-compatible, LM Studio, vLLM, etc.)
# litellm>=1.40.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
loguru>=0.7.0
